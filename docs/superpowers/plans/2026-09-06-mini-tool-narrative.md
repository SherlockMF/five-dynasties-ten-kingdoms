# 936 叙事与统一年份控件

> 收尾说明（2026-09-06）：下文为实施过程记录；其中“每组最多 6 位邻居”已被后续要求取代，当前关系网在同页展示所有一度关系，不再分页。当前交付以 mini-tool/README.md 和固定参赛快照为准。

用户已批准：移除纪年重复滑条，保留时间轨及逐年/播放；恢复 936 首页引子与原版三条阅读路线。

追加要求：人物页以可搜索的一度关系网为主体，不再默认列举人物表。使用原 seed 关系与本地画像，默认石敬瑭；点邻居切中心，中心头像打开小传，搜索匹配姓名/别名/身份。手机每组最多 6 位邻居，明确显示总数和分组，所有关系均可访问；缺少关系不虚构。保留原始阅读主线、逐站导航及事件因果入口。

约束：ES2017、Chrome 61 CSS 基线、经典离线脚本、不增依赖、不改原 Next 页面；地图仅使用现有 907—979 年数据。

执行完成：51 项小工具测试、642 项全仓测试、typecheck、lint、diff --check 通过；390px 路线/地图返回、936 默认值及单控件实测通过；320px 人物搜索切中心无页面溢出。产物重打包，官方目录与 ZIP 审计通过，仍仅有 2 MiB 建议体积警告；真实容器和 Chrome 61 未实测。

- [ ] 在 tests/mini-tool-ui.test.ts 添加首页、单一年份轨、路线同面板前后站测试，运行并确认失败。
- [ ] scripts/build-mini-tool.mjs 从 data/reading-paths.ts 导出原路线与 defaultYear=936；构建测试核对事件引用完整。
- [ ] mini-tool/src/app.js 恢复首页引子/地图及石敬瑭入口，yearControls 在纪年不生成 slider，路线使用同一事件详情内容并原位切站。
- [ ] mini-tool/src/styles.css 增加局部叙事和紧凑控件样式，保留地图滑条、图片、聊天及现有交互。
- [ ] 小工具测试、typecheck、lint、全仓回归、390px/320px 浏览器验证；打包及官方审计；更新摘要，明确真实容器未实测。
