# 地图水域与年代修订

按用户授权直接实施，保留共享轮廓和阶段缓存，不重绘全图。

1. 水域：排除现代水库；斋桑湖属天然湖泊的现代扩张例外，保留并说明近现代岸线误差。湖岸不作为政权边界。
2. 年代：已知兼并继续转移；支持有来源和说明的空归属，不按最近国家补齐。仅有 943 图上地域依据的周边面移到独立参考层，默认关闭；这不表示其政权在其他年份已灭亡。
3. 名称与图例：校正合并地域标签，清源军／平海军按 964 年区分；图例仅展示实际图层。
4. 验证：先补针对性回归，再生成 73 年数据、运行地图相关测试、类型检查及页面抽查，不进行多轮全量复审。
