# 离线交互结构恢复

用户已确认：按原网页恢复聊天与探索结构，并以可提交为硬约束。

1. 先增加聊天结构、年份联动、事件点选、详情往返测试并观察失败。
2. 固定聊天壳、独立消息滚动、双方气泡、折叠来源、底部话题与追问；保留预设内容和提示词。
3. 共用年份，滑条、逐年切换、播放；切页/隐藏/详情暂停；地图紧凑布局，滚轮与键盘控制，不恢复旧工具条。
4. 导出原人物关系与事件因果，恢复关联点击与逐层返回。
5. 小工具测试、类型检查、lint、移动端浏览器检查、构建及官方审计，交付 ZIP。

执行结果：以上五项已实现并验证。全仓 633 测试、小工具 42 测试通过，ZIP 2.15 MiB、官方审计通过（建议体积警告 1 条）。三种视口聊天无外壳溢出，关联地图返回与提示词全选通过。未完成的图形关系网、多轨可视化、完整地图图层与真实容器审核均列于最新校验摘要，未作一比一完成承诺。

仅修改 mini-tool/src、构建脚本和测试。不联网、不新增依赖、不虚构历史数据。ZIP 不超过 10 MiB，允许明确披露超过 2 MiB 建议值。地图更多图层、旧内核和真实容器验收单列，不承诺平台审核结果。
