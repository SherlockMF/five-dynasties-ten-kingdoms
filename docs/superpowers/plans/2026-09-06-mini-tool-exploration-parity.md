# 原版探索能力补齐

执行结果：三项已实现；小工具 46 项、全仓 637 项测试通过，typecheck / lint 通过；390×844 Chromium 验证人物原位切换、时间轨居中、君主跳转；重新打包和官方审计通过，ZIP 约 2.15 MiB，仅建议体积警告。真实容器与旧版 WebView 尚未实测。

用户选择实施检查报告优先级最高的三项；按原数据和现有视觉执行，不改变提交门禁。

1. 人物详情复用一个中心面板，关系切换不再叠加弹窗，保留返回记录、滚动及焦点。
2. 构建导出都城与君主时期；政权按原 yearEndRulers 规则选择年末君主（同年交接取起始年份较晚者），提供君主与当年事件入口。缺记录则明示，不推断。
3. 纪年增加事件年份轨、当前年份居中、主体多选；前史不受主体筛选影响；保留精确年份与全部年份入口。
4. 先增加失败测试，再实现；执行小工具回归、类型检查、lint、浏览器验证和 ZIP 审计。仅包内资源，ES2017，ZIP ≤10 MiB。
