import Link from "next/link";

import type { HistoricalEvent } from "@/types/history";

export function KeyEvents({ events }: { events: HistoricalEvent[] }) { const featured = ["later-liang-founded", "founding-later-jin", "later-zhou-founded", "chenqiao-mutiny"].map((id) => events.find((event) => event.id === id)).filter((event): event is HistoricalEvent => Boolean(event)); return <section className="border-y border-ink/15 py-20"><p className="text-[10px] tracking-[0.2em] text-cinnabar uppercase">Turning points</p><div className="mt-5 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><h2 className="max-w-2xl font-serif text-3xl sm:text-4xl">四个转折，串起五十三年</h2><Link href="/timeline?year=936" aria-label="探索完整时间线" className="text-sm text-cinnabar underline underline-offset-8">探索完整时间线</Link></div><div className="mt-10 grid gap-4 lg:grid-cols-4">{featured.map((event) => <Link key={event.id} href={`/explore/${event.id}?year=${event.startYear}`} className="group border-l border-ink/15 pl-5"><span className="font-serif text-4xl text-ink/20 group-hover:text-cinnabar">{event.startYear}</span><h3 className="mt-6 font-serif text-xl">{event.title}</h3><p className="mt-3 text-xs leading-6 text-muted">{event.summary}</p></Link>)}</div></section>; }
