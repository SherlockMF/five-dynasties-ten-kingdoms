import { SourceMarker } from "@/components/history/source-marker";
import type { Person } from "@/types/history";

export function PersonDetailPanel({ person }: { person: Person }) {
  return <section aria-labelledby="person-name" className="rounded-2xl bg-ink p-6 text-paper"><p className="text-[10px] tracking-[0.18em] text-gold uppercase">Person profile</p><div className="mt-4 flex flex-wrap items-baseline gap-x-3 gap-y-2"><h2 id="person-name" className="font-serif text-4xl">{person.name}</h2><SourceMarker entity={person} showLegend variant="inverse" /></div><p className="mt-2 text-xs text-paper/55">{person.birthYear ?? "?"}—{person.deathYear ?? "?"} · {person.roles.join(" / ")}</p><p className="mt-6 text-sm leading-7 text-paper/75">{person.biography ?? person.summary}</p>{person.disputedNote?.trim() ? <aside role="note" aria-label="异说" className="mt-5 rounded-xl border border-gold/30 bg-white/5 p-4"><p className="text-[10px] font-semibold tracking-[0.16em] text-gold uppercase">异说</p><p className="mt-2 text-sm leading-6 text-paper/75">{person.disputedNote}</p></aside> : null}<div className="mt-6 flex flex-wrap gap-2">{person.roles.map((role) => <span key={role} className="rounded-full border border-white/15 px-3 py-1 text-[10px] text-paper/70">{role}</span>)}</div></section>;
}
